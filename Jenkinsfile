pipeline {
  agent any
  stages {
    stage('Deploy Staging') {
      steps {
        sh 'ls -lart'
        sh 'cat values-test.yaml'
        sh 'ls helm-pipeline'
        sh 'ls -la' 
     }
   }
  }
}
